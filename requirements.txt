# Core dependencies - required for basic framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0
asyncpg>=0.29.0
numpy>=1.24.0

# Optional dependencies - install via setup.py extras:
# pip install -e .[llm]    # For LLM support (OpenAI, Anthropic, Ollama)
# pip install -e .[rag]    # For RAG support (sentence-transformers)
# pip install -e .[dev]    # For development (pytest, black)
# pip install -e .[all]    # Everything