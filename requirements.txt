# ============================================================================
# AI Agent Builder - Minimal Working Requirements
# Only packages actually used in the system
# Tested and verified on Python 3.11
# ============================================================================

# ============================================================================
# CORE DEPENDENCIES (Required for API)
# ============================================================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Data Validation
pydantic==2.5.3

# Database
psycopg2-binary==2.9.9

# Configuration
python-dotenv==1.0.1

# HTTP Requests (used by LLM providers)
requests==2.31.0

# ============================================================================
# RAG DEPENDENCIES (Required for semantic search)
# ============================================================================

# Embeddings - Sentence Transformers
sentence-transformers==2.3.1

# PyTorch (CPU version - required by sentence-transformers)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2

# Vector Stores
# ChromaDB - Latest version (auto-installs correct hnswlib)
chromadb

# FAISS - High performance search
faiss-cpu==1.7.4

# NumPy (required by FAISS and ML operations)
numpy==1.26.3

pandas
# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================

# Sentiment Analysis (used in news analysis agents)
vaderSentiment==3.3.2

# ============================================================================
# INSTALLATION
# ============================================================================

# Install everything:
# pip install --upgrade pip
# pip install -r requirements.txt

# Verify installation:
# python -c "import fastapi, sentence_transformers, chromadb, faiss; print('✅ All working!')"

# Test RAG system:
# python examples/rag_agents.py

# ============================================================================
# PACKAGE SUMMARY
# ============================================================================

# Core:                    ~150 MB
# PyTorch (CPU):           ~800 MB
# Sentence Transformers:   ~500 MB
# ChromaDB + FAISS:        ~100 MB
# Total:                   ~1.5 GB

# ============================================================================
# WORKING CONFIGURATIONS
# ============================================================================

# 1. Simple RAG (no extra dependencies needed):
#    rag = RAGEngine(db=db, embedding="simple", vectorstore="memory")

# 2. FAISS (high performance):
#    rag = RAGEngine(db=db, embedding="sentence-transformers", vectorstore="faiss")

# 3. ChromaDB (persistent storage):
#    rag = RAGEngine(db=db, embedding="sentence-transformers", vectorstore="chroma")

# ============================================================================
# NOTES
# ============================================================================

# - ChromaDB will auto-install compatible hnswlib version
# - PyTorch CPU version used for smaller download size
# - All LLM providers (Ollama, OpenAI, Anthropic) use requests library
# - No development dependencies included (add separately if needed)